/*
Your first challenge is to write a function which generates an env file
The file name should be .development.env
You need to get the values from startingValues.json and add all of them to the env file when it is being generated

So the end result in the env file should look something like (but will have different values):
PORT=3000
HOST=localhost
DB_HOST=localhost
*/

// Write your code here:
